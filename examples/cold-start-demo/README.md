# Cold-Start Demo Source

This directory is a tiny repo-local demo target for `formsmith`.

It exists for one purpose:

- let a fresh clone of `formsmith` reach meaningful output without relying on sibling benchmark repos

This is not benchmark evidence.

For benchmark evidence, use:

- `bb benchmark-v1`
- `bb validate-v1`
